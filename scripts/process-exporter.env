# Process Exporter Configuration
# This file contains environment variables for the process exporter service

# Server listening port
PORT=9999

# Server listening address (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
ADDRESS=0.0.0.0

# Log level (error, warn, info, debug, trace)
RUST_LOG=info

# Optional: Custom log format
# RUST_LOG_STYLE=always

# Optional: Backtrace on panic (0=disabled, 1=enabled, full=full backtrace)
# RUST_BACKTRACE=0